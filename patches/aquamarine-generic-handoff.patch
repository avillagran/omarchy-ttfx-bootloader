diff --git a/include/aquamarine/backend/DRM.hpp b/include/aquamarine/backend/DRM.hpp
--- a/include/aquamarine/backend/DRM.hpp
+++ b/include/aquamarine/backend/DRM.hpp
@@ -357,6 +357,8 @@ namespace Aquamarine {
         void                                           disconnect();
         Hyprutils::Memory::CSharedPointer<SDRMCRTC>    getCurrentCRTC(const drmModeConnector* connector);
         drmModeModeInfo*                               getCurrentMode();
+        // true when the kernel currently has a CTM blob set on this connector's CRTC
+        bool                                           hasActiveCTM();
         IOutput::SParsedEDID                           parseEDID(std::vector<uint8_t> data);
         bool                                           commitState(SDRMConnectorCommitData& data);
         void                                           applyCommit(SDRMConnectorCommitData& data);
diff --git a/src/backend/drm/DRM.cpp b/src/backend/drm/DRM.cpp
--- a/src/backend/drm/DRM.cpp
+++ b/src/backend/drm/DRM.cpp
@@ -58,7 +58,19 @@ static bool shouldSubmitCTM(SP<SDRMConnector> connector, const COutputState::SIn
     if (!(state.ctm == Mat3x3::identity()))
         return true;
 
-    return connector->crtc->props.values.ctm && !connector->crtc->atomic.ctmStateKnown;
+    if (!connector->crtc->props.values.ctm)
+        return false;
+
+    if (connector->crtc->atomic.ctmStateKnown)
+        return false;
+
+    // Avoid re-emitting identity CTM if no CRTC CTM blob exists. Although
+    // identity is visually neutral, some DRM drivers must reconfigure the
+    // pipe's CSC block to apply it, resulting in an avoidable modeset.
+    return connector->hasActiveCTM();
 }
@@ -1879,6 +1891,13 @@ static int32_t calculateRefresh(const drmModeModeInfo& mode) {
     return refresh;
 }
 
+bool Aquamarine::SDRMConnector::hasActiveCTM() {
+    if (!crtc || !crtc->props.values.ctm)
+        return false;
+    uint64_t cur = 0;
+    return getDRMProp(backend->gpu->fd, crtc->id, crtc->props.values.ctm, &cur) && cur != 0;
+}
+
 drmModeModeInfo* Aquamarine::SDRMConnector::getCurrentMode() {
     if (!crtc)
         return nullptr;
diff --git a/src/backend/drm/impl/Atomic.cpp b/src/backend/drm/impl/Atomic.cpp
--- a/src/backend/drm/impl/Atomic.cpp
+++ b/src/backend/drm/impl/Atomic.cpp
@@ -1,4 +1,5 @@
 #include <aquamarine/backend/drm/Atomic.hpp>
+#include "../Props.hpp"
 #include <cerrno>
 #include <cstdint>
 #include <cstring>
@@ -268,7 +269,14 @@ void Aquamarine::CDRMAtomicRequest::addConnector(Hyprutils::Memory::CSharedPoint
         // Setup HDR
         if (connector->props.values.max_bpc && connector->maxBpcBounds.at(0) && connector->maxBpcBounds.at(1) && !connector->maxBpcFailed) {
             newMaxBpc = getMaxBPC(connector->maxBpcBounds.at(0), connector->maxBpcBounds.at(1), data.mainFB->buffer->dmabuf().format);
-            if (forceConnProps || connector->atomic.maxBpc != newMaxBpc) {
+            // Do not lower a driver-provided max_bpc that already covers this
+            // framebuffer. The active link depth remains EDID-bounded, while
+            // lowering this connector property may trigger a needless modeset.
+            uint64_t curMaxBpc = 0;
+            const bool alreadyCovered = getDRMProp(backend->gpu->fd, connector->id, connector->props.values.max_bpc, &curMaxBpc) && curMaxBpc >= newMaxBpc;
+            if (!alreadyCovered && (forceConnProps || connector->atomic.maxBpc != newMaxBpc)) {
                 add(connector->id, connector->props.values.max_bpc, newMaxBpc);
                 maxBpcEmitted = true;
             }
